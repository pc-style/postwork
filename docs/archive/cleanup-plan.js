export const meta = {
  name: 'cleanup-plan',
  description: 'Implement CLEANUP_PLAN.md steps sequentially, one agent + one commit per step',
  phases: [
    { title: 'Phase 1 - Foundation' },
    { title: 'Phase 2 - Primitives' },
    { title: 'Phase 3 - Token sweep' },
    { title: 'Phase 4 - Pages & UX' },
    { title: 'Phase 5 - Architecture' },
    { title: 'Phase 6 - Backend hardening' },
    { title: 'Phase 7 - Docs & hygiene' },
  ],
}

const STEPS = [
  { id: '1.1', phase: 'Phase 1 - Foundation', title: 'Add radius tokens to the theme',
    instructions: `Add to the @theme block in src/index.css: --radius-sm: 2px; --radius-md: 6px; --radius-lg: 8px;. Do not change any component files - the existing rounded-sm/md/lg classes will pick up the new token values automatically.`,
    verify: `bun run typecheck` },
  { id: '1.2', phase: 'Phase 1 - Foundation', title: "Replace text-red-300 with the urgent token",
    instructions: `In src/components/AppShell.tsx, change the sidebar urgent-count class 'text-red-300' to 'text-urgent' (the --color-urgent theme token generates this utility). Do not touch src/components/AgentSummary.tsx's red-300 error note - that one is correct per spec and must stay.`,
    verify: `grep -rn "red-300" src/components/AppShell.tsx; echo done` },
  { id: '1.3', phase: 'Phase 1 - Foundation', title: 'Global :focus-visible + select focus styles',
    instructions: `Add a global rule to src/index.css: ":focus-visible { outline: 2px solid color-mix(in srgb, var(--color-accent) 55%, transparent); outline-offset: 2px; }". Then add the class "focus:border-accent/50" to the three <select> elements that currently only have outline-none with no focus treatment: src/components/QuickPostBar.tsx (space select), src/components/NewPostDialog.tsx (space select), src/routes/SpacePage.tsx (visibility select). Match the existing sibling text-input focus treatment on the same forms.`,
    verify: `bun run typecheck` },
  { id: '1.4', phase: 'Phase 1 - Foundation', title: 'Resolve tracked-uppercase labels',
    instructions: `DESIGN.md mandates sentence case, no tracked all-caps, except the one sanctioned agent badge (src/components/AgentTag.tsx). Remove "uppercase tracking-*" classes and switch to sentence-case label styling (e.g. text-[11px] font-medium text-muted, adjusting the label text itself to sentence case, e.g. "your queue" stays lowercase / non-tracked) at these sites: src/components/AppShell.tsx ("your queue" header), src/components/UserSwitcher.tsx (section header), src/routes/FlashExperimentsPage.tsx (the accent eyebrow AND the meta row), src/components/UserRoleTag.tsx (role tag, e.g. render "Owner" sentence case, not uppercase+tracked). Do NOT touch src/components/AgentTag.tsx - it is the one sanctioned uppercase usage. After the edit, grep -rn "uppercase" src/ should only show AgentTag.tsx.`,
    verify: `grep -rn "uppercase" src/; bun run typecheck` },
  { id: '1.5', phase: 'Phase 1 - Foundation', title: 'Fix "home" nav active state',
    instructions: `In src/components/AppShell.tsx, the sidebar nav has a bug: the "home" link's active state is computed only from whether feedPriority search param is unset, with no pathname check, so it lights up as active on every route (spaces, agents, orgs, post detail, etc) simultaneously with the actually-active route link. Fix by using TanStack Router Link's activeProps/activeOptions={{ exact: true, includeSearch: true }} consistently for ALL nav links (home, priority-filter link, and the other route nav items), deleting the manual ACTIVE class constant and the useRouterState-based priority selection logic. Exactly one nav item should be active for: "/", "/?priority=urgent", and each other route path.`,
    verify: `bun run typecheck` },
  { id: '1.6', phase: 'Phase 1 - Foundation', title: 'Make the "+ new post" CTA actually start a post',
    instructions: `In src/components/AppShell.tsx the primary "+ new post" CTA is currently a <Link to="/"> that does nothing useful. Change it to hold local "open" state in AppShell, render the existing src/components/NewPostDialog.tsx component controlled by that state, and make the CTA a <button onClick> that opens it, instead of a Link. Keep NewPostDialog's existing props/behavior intact since it is also used by two flash experiments (src/flashExperiments/experiments/wide-review-shell.tsx and centered-rail.tsx) - do not break their usage, just also wire it into AppShell.`,
    verify: `bun run typecheck` },
  { id: '2.1', phase: 'Phase 2 - Primitives', title: 'Extract Button component',
    instructions: `Create src/components/Button.tsx exporting a Button component with variant: 'primary' | 'ghost' | 'quiet' | 'pill' and an optional size prop, encoding DESIGN.md's button recipes once: primary = rounded-lg bg-accent hover:bg-accent-soft disabled:opacity-40 disabled:cursor-not-allowed (px-3 py-1.5 default sizing); ghost = rounded-md border-accent/30 text-accent-soft hover:bg-accent/15; pill = rounded-full (for the SpacePage "invite org" button - preserve its exact current visual size/padding as this variant). Then replace ALL inline primary/ghost button recipes across: src/components/Composer.tsx, src/components/QuickPostBar.tsx, src/components/AgentTasksPanel.tsx, src/components/NewPostDialog.tsx, src/components/WallPostDialog.tsx, src/components/AppShell.tsx, src/components/AgentSummary.tsx, src/routes/SpacesPage.tsx, src/routes/SpacePage.tsx, src/routes/WallPage.tsx with <Button variant=...>. This naturally reconciles the rounded-md primaries to rounded-lg per spec. Grep for "bg-accent px" across src/ afterward - it should only match Button.tsx itself (flash-experiment fork files under src/flashExperiments/experiments/ are OK to leave for now, do not touch them in this step).`,
    verify: `grep -rn "bg-accent px" src/components src/routes; bun run typecheck` },
  { id: '2.2', phase: 'Phase 2 - Primitives', title: 'Extract Chip primitive + PostMetaChips',
    instructions: `Create src/components/Chip.tsx exporting a Chip component with props {tone: 'accent'|'urgent'|'high'|'neutral'|'muted', size?, dot?: boolean, uppercase?: boolean} implementing DESIGN.md's chip recipe (rounded-md/sm border px-1.5 py-0.5 text-[10px]/[11px] family) once. Re-express src/components/StatusChip.tsx, src/components/AgentTag.tsx, src/components/UserRoleTag.tsx as thin wrappers around Chip (AgentTag keeps its sanctioned uppercase). Then create a shared src/components/PostMetaChips.tsx that renders the pinned chip + priority chip (dot + label) + space chip row, built from Chip, and use it from both src/components/PostCard.tsx and src/routes/PostPage.tsx to replace their duplicated inline versions of that row. Also convert the scope tag and role sub-badge in src/routes/SpacePage.tsx to use Chip. Leave src/routes/FlashExperimentsPage.tsx's status/slot chips and ImplementedBadge alone unless trivial - focus on PostCard/PostPage/SpacePage/the three chip components.`,
    verify: `bun run typecheck` },
  { id: '2.3', phase: 'Phase 2 - Primitives', title: 'Extract Dialog shell + PostForm',
    instructions: `Build a shared Dialog component in src/components/Dialog.tsx on the native <dialog> element (gives Escape-to-close, focus containment, and ::backdrop for free) that restores focus to the trigger element on close, with one standardized close affordance (pick either "close" text or an "x" icon button, use it everywhere). Also build src/components/PostForm.tsx encapsulating the create-post form (title input, body textarea, optional space select via a showSpace prop, priority pill row, busy "posting..." submit button, onSubmit(fields) callback). Then rewrite src/components/NewPostDialog.tsx and src/components/WallPostDialog.tsx to be thin ~20-line configurations of Dialog + PostForm (preserving each dialog's distinct behavior: WallPostDialog has no space picker and passes wallOwnerId, NewPostDialog has the space picker). Also update the "post to space" inline composer section in src/routes/SpacePage.tsx to reuse PostForm with its extra visibility select passed as children/extra field. QuickPostBar.tsx keeps its own layout (it's not a modal) but reuse PostForm's field/priority-row pieces where practical without a large rewrite. Preserve all existing store.createPost / navigate behavior exactly - this is a pure refactor, not a behavior change.`,
    verify: `bun run typecheck` },
  { id: '2.4', phase: 'Phase 2 - Primitives', title: 'Unify popover dismiss behavior', dependsOnPrior: true,
    instructions: `Create a shared hook usePopoverDismiss(ref, onClose) in src/lib/ (outside-click via mousedown/focusout + Escape key to close) and use it in both src/components/UserSwitcher.tsx and src/components/SendAgentButton.tsx, replacing their separate/missing dismiss logic. Fix UserSwitcher's menu positioning: it's anchored to a bottom-of-sidebar trigger but opens downward off-screen - change it to open upward (bottom-full mb-2 style) instead of below the trigger. Add aria-expanded/aria-haspopup="menu" to the UserSwitcher trigger button. Add Escape-closes + outside-click-closes to SendAgentButton's agent menu (it currently has neither).`,
    verify: `bun run typecheck` },
  { id: '2.5', phase: 'Phase 2 - Primitives', title: 'Dedupe OrgSquare',
    instructions: `The org color-square component "OrgSquare" is defined locally three times: src/routes/SpacePage.tsx, src/routes/SpacesPage.tsx, src/routes/LinkedOrgsPage.tsx. Create one exported src/components/OrgSquare.tsx using the most complete variant (shrink-0 class + size prop defaulting to size-6), delete the three local definitions, and import it in all three routes instead.`,
    verify: `grep -rn "function OrgSquare" src/; bun run typecheck` },
  { id: '2.6', phase: 'Phase 2 - Primitives', title: 'Dedupe the wine-framed agent panel scaffold',
    instructions: `Extract a shared src/components/AccentPanel.tsx component with props {chipLabel, title, action?, children} implementing the accent-framed panel recipe (rounded-lg border border-accent/25 bg-accent/[0.06] p-4, header with a small accent label chip + lowercase tracked title) that is currently copy-pasted in src/components/AgentSummary.tsx, src/components/AgentTasksPanel.tsx, and src/routes/AgentsPage.tsx. Replace all three call sites with AccentPanel.`,
    verify: `grep -rn "border-accent/25" src/; bun run typecheck` },
  { id: '3.1', phase: 'Phase 3 - Token sweep', title: 'Kill arbitrary [var(--color-*)] classes',
    instructions: `Do a mechanical, careful find/replace across src/ (components, routes, lib/format.ts; skip src/flashExperiments if it complicates things, but include it if straightforward) converting arbitrary Tailwind color classes to the generated theme utilities that already exist because these tokens are declared in the @theme block in src/index.css: text-[var(--color-muted)] -> text-muted, border-[var(--color-border)] -> border-border, bg-[var(--color-surface)] -> bg-surface, bg-[var(--color-bg)] -> bg-bg, text-[var(--color-faint)] -> text-faint, text-[var(--color-urgent)] -> text-urgent, text-[var(--color-high)] -> text-high, bg-[var(--color-surface-2)] -> bg-surface-2, and any opacity-suffixed variants likewise e.g. bg-[var(--color-urgent)]/10 -> bg-urgent/10. Review the diff for correctness before finishing - do not change unrelated code. After the sweep, grep -rn "var(--color-" src/ --include='*.tsx' --include='*.ts' should return nothing.`,
    verify: `grep -rln "var(--color-" src --include='*.tsx' --include='*.ts'; bun run typecheck` },
  { id: '3.2', phase: 'Phase 3 - Token sweep', title: 'Tokenize the type ramp',
    instructions: `Add to the @theme block in src/index.css: --text-label: 0.6875rem; --text-label--line-height: 1.2; --text-title: 0.9375rem; --text-title--line-height: 1.3; --text-code: 0.8125rem;. Then across src/ replace text-[11px] with text-label, text-[15px] with text-title, and text-[13px] with text-code, wherever they represent the label/title/mono ramp steps (check each site briefly - e.g. src/components/PostCard.tsx, StatusChip.tsx, AppShell.tsx, src/routes/PostPage.tsx, src/components/CodeBlock.tsx, src/routes/SpacesPage.tsx, src/routes/SpacePage.tsx). For the stray text-[10px] usages (AgentTag.tsx, UserRoleTag.tsx, AgentSummary.tsx, AgentTasksPanel.tsx, various routes) and the one text-[9px] outlier in src/routes/SpacePage.tsx: lift them up to use text-label (11px) instead, since 10px/9px are not part of the design spec's ramp - do not introduce a new badge token. After this, grep -rn "text-\\[1[015]px\\]|text-\\[9px\\]" src/ should return nothing.`,
    verify: `grep -rn "text-\\[1[015]px\\]\\|text-\\[9px\\]" src/; bun run typecheck` },
  { id: '3.3', phase: 'Phase 3 - Token sweep', title: 'One shared identity palette',
    instructions: `Identity/avatar colors are currently defined independently in three places with divergent hues, including banned blue/violet: convex/seed.ts (9 colors including #9d5cff violet), convex/discussions.ts (AVATAR_PALETTE, 6 colors including #3a6ea5 blue and #7d5ba6 violet), and src/lib/spaces.tsx (org colors + per-post authorColor literals). Create one shared palette module convex/avatarPalette.ts exporting a warm-muted color array (drop #3a6ea5 blue and #9d5cff violet entirely - keep the wine/teal/gold/moss family colors that remain), and import it from convex/seed.ts, convex/discussions.ts, and src/lib/spaces.tsx (client code can import from convex/ in this project). Note the seed data only applies on reseed, that's fine. Also add a brief note to DESIGN.md's color section pointing at this palette module. After this, grep -rn "#3a6ea5|#9d5cff" convex/ src/ should return nothing.`,
    verify: `grep -rn "#3a6ea5\\|#9d5cff" convex/ src/; bun run typecheck` },
  { id: '4.1', phase: 'Phase 4 - Pages & UX', title: 'Shared page-header/back-link scaffold',
    instructions: `Create src/components/PageHeader.tsx with props (backTo, backLabel, title, description?, action? slot), normalizing to text-xl titles and lowercase back-link labels per DESIGN.md's lowercase-nav rule. Replace the six hand-rolled back-link + header blocks in: src/routes/PostPage.tsx, src/routes/AgentsPage.tsx, src/routes/SpacesPage.tsx, src/routes/SpacePage.tsx (currently capitalized "← Spaces" - lowercase it), src/routes/WallPage.tsx, src/routes/LinkedOrgsPage.tsx, and also normalize src/routes/FlashExperimentsPage.tsx's text-2xl title to text-xl if it has its own header block that fits the pattern.`,
    verify: `bun run typecheck` },
  { id: '4.2', phase: 'Phase 4 - Pages & UX', title: 'Consistent loading/empty states',
    instructions: `Create src/components/LoadingState.tsx and src/components/EmptyState.tsx with one shared visual treatment (dashed hairline card for empty, muted lowercase centered text for loading, matching the app's lowercase register). Replace the divergent loading/empty implementations in src/routes/FeedPage.tsx, src/routes/PostPage.tsx, src/routes/WallPage.tsx, src/routes/AgentsPage.tsx, src/routes/LinkedOrgsPage.tsx. Add a missing empty-state branch to src/routes/SpacePage.tsx for when a space has zero posts (currently renders an empty gap). Delete the unreachable dead empty-state branch in src/routes/FlashExperimentsPage.tsx (the one that can never render because items.length === 0 already returns null earlier).`,
    verify: `bun run typecheck` },
  { id: '4.3', phase: 'Phase 4 - Pages & UX', title: 'Per-route document titles',
    instructions: `Add a small useDocumentTitle(title: string) hook (e.g. in src/lib/useDocumentTitle.ts) that sets document.title on mount/update and restores the default "Postwork — async team communication" on unmount. Call it from page route components with an appropriate title, e.g. src/routes/PostPage.tsx should use "\${post.title} · postwork" once the post has loaded (fall back to a generic title while loading), and give the other routes (SpacePage, WallPage, AgentsPage, SpacesPage, etc) sensible per-page titles too, e.g. "spaces · postwork".`,
    verify: `grep -rn "document.title\\|useDocumentTitle" src/; bun run typecheck` },
  { id: '5.1', phase: 'Phase 5 - Architecture', title: 'Restructure the overlay store', critical: true,
    instructions: `In src/lib/store.tsx, StoreProvider currently builds its context value object literal fresh on every render with no useMemo, and exposes query hooks (useFeed/useSearch/usePost/useReplies/useCounts/useWall) as closures inside the provider body invoked via the context object (store.useFeed(...) etc at call sites like src/routes/FeedPage.tsx and src/components/AppShell.tsx). Refactor to: keep a narrow context holding only overlay state + the stable useCallback mutators (markRead, markAllRead, createReply, createPost, summarize, applyOverlay, enrichSessionPost), wrap that context value in useMemo, and move useFeed/usePost/useReplies/useWall/useSearch/useCounts to module-level EXPORTED hooks in store.tsx that call useQuery + useContext/useStore() directly instead of going through the context object. Update every call site (e.g. store.useFeed(...) -> useFeed(...) imported directly) across src/routes and src/components. This must be a pure refactor with NO behavior change - the session-overlay semantics documented at the top of store.tsx must be preserved exactly. Run bun run typecheck repeatedly while doing this since it's the riskiest step in the whole plan - if anything doesn't compile cleanly, stop and get it working before moving on.`,
    verify: `bun run typecheck` },
  { id: '5.2', phase: 'Phase 5 - Architecture', title: 'Single Priority type, typed search params',
    instructions: `The priority literal union ("urgent"|"high"|...) is redeclared in convex/schema.ts (the priority validator), a private "type Priority" in src/lib/store.tsx, three inline repeats in src/lib/spaces.tsx, and PRIORITIES const in src/lib/format.ts. Export one canonical "export type Priority = (typeof PRIORITIES)[number]" from src/lib/types.ts and use it everywhere (store.tsx, spaces.tsx). Type the router's FeedSearch.priority field (in src/router.tsx) as "Priority | undefined" instead of plain string, doing the narrowing inside validateSearch, so the "as never" casts in src/routes/FeedPage.tsx (search.priority as never) and src/lib/store.tsx (args.priority as never) can be deleted. Also change priorityStyles in src/lib/format.ts from Record<string,...> to Record<Priority,...>, and simplify "type SessionReply" in store.tsx to just "type SessionReply = Doc<\"replies\">" if it's currently a manual field-by-field redeclaration. After this, grep -rn "as never" src/ should return nothing.`,
    verify: `grep -rn "as never" src/; bun run typecheck` },
  { id: '5.3', phase: 'Phase 5 - Architecture', title: 'Honest local-id handling',
    instructions: `In src/lib/store.tsx, the overlay mints synthetic local ids via "as unknown as Id<...>" double-casts in two places. Centralize this into one exported helper, e.g. "function makeLocalId<T extends string>(table: T, n: number)" with the cast and a short comment explaining the local_ convention, and use it at both call sites instead of inline casts. Also export the existing isLocalId function directly from store.tsx ("export { isLocalId }" or "export function isLocalId") instead of exposing it through the StoreValue context object and threading it as a prop into src/routes/AgentsPage.tsx and src/components/AgentSummary.tsx - update those two files to import isLocalId directly instead of receiving it as a prop.`,
    verify: `grep -rn "as unknown as Id" src/; bun run typecheck` },
  { id: '5.4', phase: 'Phase 5 - Architecture', title: 'Flatten/document the provider pyramid',
    instructions: `In src/main.tsx, the five nested providers (ConvexProviderWithAuth > SessionProvider > StoreProvider > AgentTasksProvider > SpacesProvider > ExperimentProvider) have an undocumented ordering dependency (AgentTasksProvider needs useStore()+useSession(), SpacesProvider needs useSession(), StoreProvider needs useSession()). At minimum, add a clear comment above the nesting in main.tsx documenting the required order and why. If it's easy to do without much risk, also extract this into one AppProviders component (e.g. src/lib/providers.tsx) with the comment there instead, and use it from main.tsx - but the comment alone satisfies this step if time is short.`,
    verify: `bun run typecheck` },
  { id: '5.5', phase: 'Phase 5 - Architecture', title: 'Fix graduated flash experiments (has a live double-composer bug)', critical: true,
    instructions: `Two "graduated"/"shipped" flash experiments keep full stale forks of code instead of rendering the real shipped components: src/flashExperiments/experiments/centered-rail.tsx forks AppShell and has already drifted (still opens the old NewPostDialog flow, missing current nav items), and src/flashExperiments/experiments/inline-bottom-composer.tsx forks QuickPostBar as a pre-polish version. There is a LIVE BUG: src/routes/FeedPage.tsx unconditionally renders both {slots.feedHeader} AND <QuickPostBar />, so activating the inline-bottom-composer experiment mounts two overlapping fixed bottom-docked composers at once. Fix: point graduated/shipped experiments' appSlots at the real shipped components (e.g. shell: AppShell) instead of stale forks, or drop their appSlots entirely if the ImplementedRow archive already documents them. Make src/routes/FeedPage.tsx suppress rendering its own <QuickPostBar /> whenever the active experiment's feedHeader slot already supplies a composer. Verify: activating inline-bottom-composer must render exactly one bottom composer, never two.`,
    verify: `bun run typecheck` },
  { id: '5.6', phase: 'Phase 5 - Architecture', title: 'Reuse thread components in ExperimentDiscussion',
    instructions: `src/flashExperiments/ExperimentDiscussion.tsx re-implements reply-tree building (buildTree) and a reply composer that duplicate src/components/ReplyTree.tsx and src/components/Composer.tsx. Move the tree-building algorithm (parent-map/roots over {_id, parentId} items) into a shared generic function in src/lib/ (e.g. src/lib/replyTree.ts) and import it from both ReplyTree.tsx and ExperimentDiscussion.tsx. Extract the shared textarea + "cmd/ctrl+enter" hint + submit button row into a small ComposerShell component that both components/Composer.tsx and ExperimentDiscussion's composer render around their own distinct submit handlers (auth-gating and mutation differ genuinely, keep those separate).`,
    verify: `bun run typecheck` },
  { id: '5.7', phase: 'Phase 5 - Architecture', title: 'Derive ExperimentSlot from ExperimentSlots',
    instructions: `In src/flashExperiments/registry.tsx, the hand-written "ExperimentSlot" string union has already diverged from the real appSlots keys type (keyof ExperimentSlots in src/flashExperiments/slots.ts) - e.g. tag "app-shell" vs actual key "shell". Delete the ExperimentSlot union and change it to "type ExperimentSlot = keyof ExperimentSlots". Delete each experiment's hand-maintained "slots: [...]" display-tag field, and instead derive the display tags at render time in src/routes/FlashExperimentsPage.tsx from Object.keys(experiment.appSlots), with a small label map (e.g. { shell: "app-shell" }) if a display name should differ from the raw key.`,
    verify: `bun run typecheck` },
  { id: '6.3', phase: 'Phase 6 - Backend hardening', title: 'Backend hardening follow-ups', critical: true,
    instructions: `Three follow-ups on the Convex backend (read convex/_generated/ai/guidelines.md first per CLAUDE.md). (1) convex/users.ts strips the "subject" field in users.list, but full user docs (with subject) still leak through enrichment in convex/posts.ts (feed/get/search, author+participants), convex/replies.ts (listForPost), and convex/discussions.ts (getThread). Add one exported "publicUser()" projection function in convex/users.ts that omits subject, and apply it everywhere a user doc is attached to an enriched response in those files. subject is optional in the schema so this is type-safe. (2) Leave flashExperiments.listVotes as-is (it's fine at demo scale per the plan, just don't touch it - only add a comment if you want). (3) Do not attempt to regenerate convex/_generated/ (no configured deployment available in this environment) - skip that part.`,
    verify: `bun run typecheck` },
  { id: '7.1', phase: 'Phase 7 - Docs & hygiene', title: 'Reconcile DESIGN.md with the shipped shell',
    instructions: `Rewrite DESIGN.md's Header/Nav section and the Elevation paragraph, which currently describe a retired sticky glass top header, to instead describe the actual shipped three-column sidebar shell in src/components/AppShell.tsx (left nav rail, right "your queue" panel, user switcher at bottom of left rail, "+ new post" CTA placement). Also fix DESIGN.md's frontmatter recipes that contradict both the prose and the code: agent-summary and tag-scope currently claim solid deep-wine (#8c1862) fills in frontmatter, but both the prose and the real components use a hairline wine border over a 6-10% wash (border-accent/25 bg-accent/[0.06] for agent-summary, border-accent/30 bg-accent/10 for tag-scope) - update the frontmatter to match. Align the prose's "accent/5 fill" for the scope tag to accent/10 to match code. Since --radius-sm is now 2px (step 1.1) and uppercase labels were resolved (step 1.4) and the type ramp now has a text-label step (step 3.2), make sure DESIGN.md's rounded.sm value and uppercase/label guidance match whatever those steps actually landed on - check the current code/index.css state rather than assuming.`,
    verify: `echo "manual doc review - no automated verify"` },
  { id: '7.2', phase: 'Phase 7 - Docs & hygiene', title: 'Purge the retired monospace aesthetic from docs',
    instructions: `AGENTS.md and PRODUCT.md still describe a retired "monospace everywhere / terminal aesthetic" that contradicts DESIGN.md and the actual src/index.css (which uses Inter/--font-sans for body text, with --font-mono scoped only to code/pre/kbd/samp). Rewrite AGENTS.md's design-conventions section (currently says "Monospace everywhere. This is the defining trait") to describe the Inter humanist sans system with mono scoped to code. Update the three similar phrases in PRODUCT.md (brand description, surface description, principles list) to match: "warm near-black, Inter, one wine accent" instead of "terminal-adjacent, monospace". After this, grep -rn "Monospace everywhere" *.md should return nothing.`,
    verify: `grep -rn "Monospace everywhere" *.md; echo done` },
  { id: '7.3', phase: 'Phase 7 - Docs & hygiene', title: 'Refresh README',
    instructions: `README.md's "Project layout" section lists files/functions that no longer exist (reads.ts, posts.ts create/counts) and omits real modules (agentTasks.ts, discussions.ts, flashExperiments.ts, spaces.ts, users.ts, auth.config.ts) and undercounts routes/components. Regenerate that section to match the actual current directory contents of convex/, src/routes/, src/components/. Fix the sentence saying "use the avatar menu (top-right) to switch teammates" - UserSwitcher now sits at the bottom of the left sidebar. Also check src/components/AgentSummary.tsx's missing-API-key hint text - if it currently suggests PIONEER_API_KEY first, change it to lead with OPENAI_API_KEY to match convex/ai.ts's actual default provider order and README's documented setup.`,
    verify: `echo "manual doc review - no automated verify"` },
  { id: '7.4', phase: 'Phase 7 - Docs & hygiene', title: 'Regenerate OG images',
    instructions: `public/og.svg currently uses an SF Mono/JetBrains Mono monospace font stack (the retired terminal look) and brands the card "postwork.app" while index.html's og:url/og:image meta tags declare https://postwork.pcstyle.dev/. Rebuild public/og.svg to use the Inter font stack matching --font-sans in src/index.css, fix the domain text to postwork.pcstyle.dev (or remove the domain string if simpler), and reduce the wordmark tile's corner radius from rx=20 down to something <=8 per DESIGN.md's radius rule. If you cannot regenerate the PNG raster (no image tooling available), just fix the SVG source and leave a brief note in the commit message that og.png needs re-exporting from og.svg by a human with image tooling.`,
    verify: `grep -n "font-family" public/og.svg; grep -n "postwork.app" public/og.svg; echo done` },
  { id: '7.6', phase: 'Phase 7 - Docs & hygiene', title: 'Pin dependencies', critical: true,
    instructions: `package.json currently pins several dependencies to the "latest" or "beta" dist-tags (react, react-dom, @tanstack/react-router, convex, tailwindcss, @tailwindcss/vite, vite, typescript, @types/react, @types/react-dom, @vitejs/plugin-react, npm-run-all as "latest"; ai, @ai-sdk/gateway, @ai-sdk/openai-compatible as "beta"). Read bun.lock to find the exact versions currently resolved for each of these packages, then replace the dist-tags in package.json with caret ranges for the stable packages (e.g. "^4.3.1") and exact pinned versions for the beta packages (e.g. "7.0.0-beta.184") matching whatever bun.lock actually shows. Then run "bun install" and confirm bun.lock is unchanged or only trivially reordered - do not let it silently upgrade anything.`,
    verify: `grep -c '"latest"\\|"beta"' package.json; bun install; bun run typecheck` },
]

for (const step of STEPS) {
  phase(step.phase)
  log(`Step ${step.id}: ${step.title}`)
  const prompt = `You are implementing one step of a larger cleanup plan for the "postwork" repo at the current working directory. This is step ${step.id} ("${step.title}") from CLEANUP_PLAN.md - read that file's section for step ${step.id} for full context/rationale if useful, but the concrete instructions below are authoritative and self-contained.

IMPORTANT: Before touching anything under convex/, read convex/_generated/ai/guidelines.md first (per this repo's CLAUDE.md) - it overrides trained Convex knowledge.

Task:
${step.instructions}

Process:
1. Use grep/read to locate the exact current code (line numbers in the plan doc are stale - do not trust them blindly).
2. Make the edit(s) described above. Do not do unrelated cleanup, do not add comments explaining what you changed, do not add speculative abstractions beyond what's asked.
3. Run: ${step.verify}
4. Run "bun run typecheck" if not already covered above, and make sure it passes with zero errors. If it fails, fix it - do not leave a broken build.
5. If everything passes, stage ONLY the files relevant to this step with "git add <specific files>" (never "git add -A" or "git add ."), then create a git commit with a concise message describing this step, formatted as:
   git commit -m "$(cat <<'EOF'
   <concise imperative summary of step ${step.id}>

   EOF
   )"
6. If you get stuck or typecheck cannot be made to pass after reasonable effort, do NOT commit - instead return a clear explanation of what's blocking you and leave the working tree as-is (uncommitted) so the state is inspectable.

Report back concisely: what you changed, whether you committed, and the commit hash if so.`
  const result = await agent(prompt, { label: `step-${step.id}`, phase: step.phase })
  log(`Step ${step.id} result: ${typeof result === 'string' ? result.slice(0, 300) : JSON.stringify(result).slice(0, 300)}`)
}

return { done: true, stepsRun: STEPS.map(s => s.id) }
