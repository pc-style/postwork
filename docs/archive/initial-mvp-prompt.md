Build an MVP for a post-based team communication app. Use Bun only. Start from the included Theo project brief. Use these stack choices unless the brief reveals a strong reason to adjust: Convex for realtime/persistence, Bun for tooling, Vite/React for the app loop, and TanStack Router if useful for structure. Optimize for fastest demo while keeping architecture close to the final product. Core flow: posts as the top-level unit instead of channels, nested replies, activity bumping, search, unread/priority states, and an agent-summary slot on each post. Include seed data that makes the async-post model feel clearly better than Slack-style chat.
